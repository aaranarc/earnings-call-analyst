services:
  - type: web
    name: aarana-fastapi-backend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    plan: free

  - type: web
    name: aarana-earnings-frontend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerCommand: streamlit run app/ui/streamlit_app.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: API_URL
        value: https://aarana-fastapi-backend.onrender.com
    plan: free
